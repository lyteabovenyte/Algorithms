def in_spiral_order(f):
    def wrapper(*args, **kwargs):
        driection = x = y = 0
        result = []
        shift = ((0, 1), (1, 0), (0, -1), (-1, 0))
        for i in range(d ** 2 + 1):
            result[x][y] = i
            next_x, next_y = 

